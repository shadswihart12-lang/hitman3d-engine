# Hitman3D Studio Gem

All custom studio systems live here. This Gem survives upstream syncs.

## Conventions
- Each subsystem gets its own subdir: Renderer/, Netcode/, AI/, etc.
- Use H3D_ prefix for studio identifiers (NOT O3DE_ — that's upstream).
